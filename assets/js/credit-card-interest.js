const $=id=>document.getElementById(id);const n=id=>Number($(id).value)||0;const usd=v=>new Intl.NumberFormat((window.DCHI18N&&window.DCHI18N.getLocale?window.DCHI18N.getLocale():'en-US'),{style:'currency',currency:'USD',currencyDisplay:'narrowSymbol',useGrouping:true,maximumFractionDigits:2}).format(v);const pct=v=>`${v.toFixed(2)}%`;function calc(){let b=n('balance'),r=n('apr')/1200,p=n('payment'),months=0,total=0,first=b*r;if(p<=b*r&&b>0){$('mainResult').textContent='Payment too low';$('firstInterest').textContent=usd(first);$('totalInterest').textContent='—';return}while(b>0&&months<1200){const i=b*r,totalPay=Math.min(p,b+i);total+=i;b=b+i-totalPay;months++}$('mainResult').textContent=`${months} months`;$('firstInterest').textContent=usd(first);$('totalInterest').textContent=usd(total)}$('calculateButton').onclick=calc;calc();