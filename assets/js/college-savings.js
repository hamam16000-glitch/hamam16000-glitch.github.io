const $=id=>document.getElementById(id);const n=id=>Number($(id).value)||0;const usd=v=>new Intl.NumberFormat((window.DCHI18N&&window.DCHI18N.getLocale?window.DCHI18N.getLocale():'en-US'),{style:'currency',currency:'USD',currencyDisplay:'narrowSymbol',useGrouping:true,maximumFractionDigits:2}).format(v);const pct=v=>`${v.toFixed(2)}%`;function calc(){const goal=n('goal'),cur=n('current'),m=n('years')*12,r=n('return')/1200,fvCur=cur*Math.pow(1+r,m),need=Math.max(0,goal-fvCur),pmt=r?need*r/(Math.pow(1+r,m)-1):need/m;$('mainResult').textContent=usd(pmt);$('futureCurrent').textContent=usd(fvCur);$('contributions').textContent=usd(pmt*m)}$('calculateButton').onclick=calc;calc();