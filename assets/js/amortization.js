const $=id=>document.getElementById(id);const n=id=>Number($(id).value)||0;const usd=v=>new Intl.NumberFormat((window.DCHI18N&&window.DCHI18N.getLocale?window.DCHI18N.getLocale():'en-US'),{style:'currency',currency:'USD',currencyDisplay:'narrowSymbol',useGrouping:true,maximumFractionDigits:2}).format(v);const pct=v=>`${v.toFixed(2)}%`;function calc(){const P=n('principal'),r=n('rate')/1200,m=n('years')*12,k=Math.min(m,n('elapsed')*12),pay=r?P*r/(1-Math.pow(1+r,-m)):P/m,bal=r?P*Math.pow(1+r,k)-pay*(Math.pow(1+r,k)-1)/r:P-pay*k;$('mainResult').textContent=usd(pay);$('totalInterest').textContent=usd(pay*m-P);$('remaining').textContent=usd(Math.max(0,bal))}$('calculateButton').onclick=calc;calc();