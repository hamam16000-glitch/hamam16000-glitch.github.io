/*
 * Daily Calculator Hub analytics configuration.
 *
 * To activate Google Analytics 4, replace the empty value below with the
 * Measurement ID from your GA4 web data stream. Example format: G-ABC123XYZ0
 *
 * Keep allowLocalhost false on the published build so local testing does not
 * pollute production reports.
 */
window.DCH_ANALYTICS_CONFIG = Object.freeze({
  googleAnalyticsMeasurementId: "",
  allowLocalhost: false
});
