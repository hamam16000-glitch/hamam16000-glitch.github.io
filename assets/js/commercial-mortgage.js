const $=id=>document.getElementById(id);const n=id=>Number($(id).value)||0;const usd=v=>new Intl.NumberFormat((window.DCHI18N&&window.DCHI18N.getLocale?window.DCHI18N.getLocale():'en-US'),{style:'currency',currency:'USD',currencyDisplay:'narrowSymbol',useGrouping:true,maximumFractionDigits:2}).format(v);const pct=v=>`${v.toFixed(2)}%`;function calc(){const P=n('amount'),r=n('rate')/1200,m=n('amortYears')*12,k=n('termYears')*12,pay=r?P*r/(1-Math.pow(1+r,-m)):P/m,bal=r?P*Math.pow(1+r,k)-pay*(Math.pow(1+r,k)-1)/r:P-pay*k;$('mainResult').textContent=usd(pay);$('balloon').textContent=usd(Math.max(0,bal));$('paid').textContent=usd(pay*k)}$('calculateButton').onclick=calc;calc();