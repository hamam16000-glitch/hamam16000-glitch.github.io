const $=id=>document.getElementById(id);const n=id=>Number($(id).value)||0;const usd=v=>new Intl.NumberFormat((window.DCHI18N&&window.DCHI18N.getLocale?window.DCHI18N.getLocale():'en-US'),{style:'currency',currency:'USD',currencyDisplay:'narrowSymbol',useGrouping:true,maximumFractionDigits:2}).format(v);const pct=v=>`${v.toFixed(2)}%`;function calc(){const g=$('grades').value.split(',').map(Number),c=$('credits').value.split(',').map(Number);let qp=0,cr=0;for(let i=0;i<Math.min(g.length,c.length);i++)if(Number.isFinite(g[i])&&Number.isFinite(c[i])&&c[i]>0){qp+=g[i]*c[i];cr+=c[i]}$('mainResult').textContent=cr?(qp/cr).toFixed(3):'Enter values';$('creditsTotal').textContent=cr.toFixed(1);$('qualityPoints').textContent=qp.toFixed(2)}$('calculateButton').onclick=calc;calc();